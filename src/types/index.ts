/**
 * Client-side type surface.
 *
 * Re-exports the shared contract so components import from one place and the
 * client can never drift from the server's definition of a trade or a signal.
 */

export * from '../../shared/types';
